# Runtime dependencies only. Development tooling lives in requirements-dev.txt
# so a Raspberry Pi never installs a test runner it will not use.
deep-translator==1.11.4
playwright==1.54.0
pymupdf==1.26.5
python-docx==1.2.0
requests==2.33.0
rich==15.0.0

